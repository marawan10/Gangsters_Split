# Gangsters Split

Shared expense tracker for the squad — now **Android native** (Expo).

The legacy Vite PWA in `src/` is deprecated. Use the app in `mobile/`.

```bash
cd mobile
npm install
npm run android
```

See [mobile/README.md](mobile/README.md) for full setup, Firebase, sounds, and production builds.
